namespace AbpWebSite.Web.Controllers
{
    public class ProjectCreator
    {
        public byte[] Create(string templateFolder, string placeHolder, string projectName)
        {
            //TODO:

            //Copy template to a temporary folder
            //Rename template in temporary folder
            //Zip the renames folder
            //Get bytes of the zip file
            //Delete the remporary file

            return null;
        }
    }
}